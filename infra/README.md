# Infrastructure

Deployment manifests and platform configuration will live here as environments are introduced. The initial foundation targets Vercel for the frontend, Railway or Render for the API, and Neon PostgreSQL for persistence.
